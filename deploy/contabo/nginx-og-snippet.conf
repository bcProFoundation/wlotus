# Open Graph routing for dedication share URLs (/<64-hex-txid>).
#
# Always proxy share URLs to dana-index OG HTML (altar name + brand fallback).
# The OG page boots the SPA in real browsers via JS; crawlers keep the meta tags.
# If dana-index is down, fall back to the SPA shell.
#
# Paste the `location` blocks into EACH HTTPS (and HTTP, if used) server block
# that serves the SPA — Certbot’s 443 block is easy to miss.
#
#    location /index-api/ {
#        proxy_pass http://127.0.0.1:8788/;
#        proxy_http_version 1.1;
#        proxy_set_header Host $host;
#        proxy_set_header X-Real-IP $remote_addr;
#        proxy_set_header X-Forwarded-Proto $scheme;
#        proxy_set_header X-Forwarded-Host $host;
#        proxy_read_timeout 120s;
#    }
#
#    # Direct OG (debug / Sharing Debugger); also used by rewrite below.
#    location ~* "^/og/([0-9a-fA-F]{64})/?$" {
#        proxy_pass http://127.0.0.1:8788;
#        proxy_http_version 1.1;
#        proxy_set_header Host $host;
#        proxy_set_header X-Real-IP $remote_addr;
#        proxy_set_header X-Forwarded-Proto $scheme;
#        proxy_set_header X-Forwarded-Host $host;
#        proxy_set_header Accept-Language $http_accept_language;
#        proxy_read_timeout 120s;
#    }
#
#    location ~* "^/([0-9a-fA-F]{64})/?$" {
#        error_page 502 503 504 = @wlotus_spa;
#        rewrite ^/([0-9a-fA-F]{64})/?$ /og/$1$is_args$args break;
#        proxy_pass http://127.0.0.1:8788;
#        proxy_http_version 1.1;
#        proxy_set_header Host $host;
#        proxy_set_header X-Real-IP $remote_addr;
#        proxy_set_header X-Forwarded-Proto $scheme;
#        proxy_set_header X-Forwarded-Host $host;
#        proxy_set_header Accept-Language $http_accept_language;
#        proxy_read_timeout 120s;
#    }
#
#    location @wlotus_spa {
#        try_files /index.html =404;
#    }
